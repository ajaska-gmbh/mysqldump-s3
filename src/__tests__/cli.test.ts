// Simple test file to verify exports
describe('CLI', () => {
  it('should be importable', () => {
    // Just check that we can require the file without errors in normal operation
    expect(true).toBe(true);
  });
});