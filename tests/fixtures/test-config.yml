# Test configuration file for integration tests
# This file is used when running tests locally or in CI

database:
  host: 127.0.0.1
  port: 3306
  user: testuser
  password: testpass
  database: testdb

s3:
  accessKeyId: minioadmin
  secretAccessKey: minioadmin
  region: us-east-1
  bucket: test-backup-bucket
  endpointUrl: http://127.0.0.1:9000

verbose: true